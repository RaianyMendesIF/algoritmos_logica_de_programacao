# Implemente uma fun¸c˜ao que receba uma lista e retorne os elementos na ordem inversa.

def inverter_lista(lista):
    lista.reverse()
    return lista

lista = [1, 2, 3, 4]
print(inverter_lista(lista))